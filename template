conky.config = {
-- Yasno power outages

-- Conky settings #
	background = false,
	update_interval = 60,
	double_buffer = true,
	no_buffers = true,

-- Window specifications #
	own_window = true,
	own_window_transparent = true,
	own_window_argb_visual = false,
	own_window_class = 'override',
	own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

	minimum_width = 620,
	maximum_width = 1241,

	minimum_height = 877,

-- Alignment #
	alignment = 'top_left',
	gap_x = 100,
	gap_y = 60,

	border_inner_margin = 10,
	border_outer_margin = 10,

-- Graphics settings #
	draw_shades = false,
	draw_outline = false,
	draw_borders = true,
	draw_graph_borders = false,

	lua_load = '~/conky-yasno/scripts/conky_set_line.lua',
	lua_draw_hook_pre = 'conky_set_line',
};

conky.text = [[
${execpi 3600 ~/conky-yasno/scripts/background.sh}
${exec ~/conky-yasno/scripts/status.sh}
${exec ~/conky-yasno/scripts/line.sh}
${image ~/conky-yasno/tmp/yasno.png -p 0,0 -s 620x877}
${lua_parse conky_set_line}
]];
